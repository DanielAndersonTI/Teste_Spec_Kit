/**
 * Jest Setup File
 * Configurações iniciais para os testes
 */

// Aqui você pode colocar configurações globais de testes
// Por exemplo: mocks globais, fixtures, etc.

// Para verificar que os testes estão funcionando
describe('Setup Test', () => {
  test('Jest está funcionando corretamente', () => {
    expect(true).toBe(true);
  });
});
